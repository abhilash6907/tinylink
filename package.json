{
  "name": "tinylink",
  "version": "1.0.0",
  "description": "TinyLink – production-ready URL shortener for NeonDB + Express",
  "type": "module",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "set NODE_OPTIONS=--dns-result-order=ipv4first&& nodemon server.js",
    "lint": "node --check server.js",
    "test": "npm run lint",
    "check": "npm run lint"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "ejs": "^3.1.10",
    "express": "^4.19.2",
    "express-ejs-layouts": "^2.5.1",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
